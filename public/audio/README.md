Drop the background music track here as **`background-music.mp3`** (this exact filename).

- Loops automatically, starts paused (browsers block autoplaying audio with sound) — the guest clicks the speaker button in the bottom-right corner to start it.
- Keep the file reasonably small (a few MB) since every guest's browser downloads it once they hit play.
