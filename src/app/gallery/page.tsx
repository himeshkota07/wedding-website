import PageSection from "@/components/PageSection";

export default function GalleryPage() {
  return (
    <PageSection
      title="Gallery"
      subtitle="Photos before, during, and after"
    >
      <p>[Placeholder — pre-wedding photos, live updates during the week, and the guest photo upload wall (Cloudinary).]</p>
    </PageSection>
  );
}
