import PageSection from "@/components/PageSection";

export default function EventsPage() {
  return (
    <PageSection
      title="Events"
      subtitle="One entry per function — Mehendi, Haldi, Sangeet, Wedding, Reception"
    >
      <p>
        [Placeholder — this will list each event pulled from Supabase: name,
        date &amp; time, venue, dress code, special instructions, a live
        countdown, and an &quot;Add to Calendar&quot; button.]
      </p>
    </PageSection>
  );
}
