import PageSection from "@/components/PageSection";

export default function VenuePage() {
  return (
    <PageSection
      title="Venue & Location"
      subtitle="Getting guests to the right place"
    >
      <p>[Placeholder — address, embedded map, parking, accessibility, nearby landmarks.]</p>
    </PageSection>
  );
}
